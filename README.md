# WorkFlow.AI
WorkFlow.AI Backend is a FastAPI-based service that automates email workflows using Google APIs and AI. It securely connects to a user’s Google account, processes emails in the background, extracts useful information, and exposes clean APIs for any frontend to consume.  This repository contains only backend logic
